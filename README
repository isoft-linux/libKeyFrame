libKeyFrame

Seek key frame and decode to raw data.


Dependence

sudo pacman -S ffmpeg


Build

./autogen.sh --prefix=/usr --enable-debug=on
make
sudo make install


Testcase

./test /data/download/isoft.avi(mov, mpg, f4v, mp4, wmv, m4v, mpeg, mkv, rmvb) /tmp
