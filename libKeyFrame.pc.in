prefix=${prefix}
exec_prefix=${exec_prefix}
libdir=${libdir}
includedir=${includedir}

Name: libKeyFrame
Description: Seek key frame from video and save to png
Version: ${VERSION}
Requires: ${LIBKEYFRAME_PACKAGES}
Libs: -L${libdir} -lKeyFrame
Cflags: -I${includedir}
